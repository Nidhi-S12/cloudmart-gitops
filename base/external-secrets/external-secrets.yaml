apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: product-service-secret
  namespace: cloudmart
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: product-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: cloudmart/product-service
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: order-service-secret
  namespace: cloudmart
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: order-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: cloudmart/order-service
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-pull-secret
  namespace: cloudmart
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: ghcr-pull-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"ghcr.io":{"username":"{{ .username }}","password":"{{ .password }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  data:
    - secretKey: username
      remoteRef:
        key: cloudmart/ghcr-pull
        property: username
    - secretKey: password
      remoteRef:
        key: cloudmart/ghcr-pull
        property: password
